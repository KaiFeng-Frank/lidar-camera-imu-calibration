# D435i + MID-360S 标定结果


> 本文是 v0.1(D435i 实例)的完整实录;项目已完成范围与发布历史 → [ROADMAP.md](ROADMAP.md)
> 误差→建图/定位/发表价值的影响分析(含 Mid-360 与雷视外参)→ [IMPACT_ANALYSIS.md](IMPACT_ANALYSIS.md)
设备 `RealSense D435I`  序列号 `947122070908`  固件 `5.12.7.100`
USB 链路 **3.2 / 5000 Mbps**(初期 USB2 是线缆问题,已换线解决,详见"已知限制")

---

## MID-360S + D435i 当前 rig 收口（2026-09-01）✅

文档刚性安装的四项 MID-360S 结果已全部固结：

| 项目 | 机器可读结果 | 当前 rig 结论 |
|---|---|---|
| MID-360S IMU 内参 | `results/mid360s_imu.json` | 17 姿态（14 fit + 3 独立 holdout）；fit/holdout RMS 0.00962/0.01194 m/s²，rank 9/9，condition 2.02 |
| LiDAR–D435i 6DoF 外参 | `results/mid360s_d435i_extrinsic.local.json` | scene06–scene10 五场景稠密对齐；inlier median/P90 10.019/16.873 mm，多起点收敛 0.00720° / 0.120 mm |
| LiDAR–内置 IMU | `results/mid360s_lidar_imu.json` | 官方定义轴向同基，`p_lidar = T_lidar_imu p_imu`，IMU 在点云坐标系中为 `[+11.00,+23.29,−44.12] mm` |
| LiDAR–D435i 常数时偏 | `results/mid360s_d435i_timesync.json` | dual-gyro 对齐得 `t_d435i_imu = t_livox_imu − 1.940 ms`；与已冻结 D435i 深度相机–IMU 时移合成后，查看器使用 `t_depth = t_livox − 5.989 ms` |

MID-360S IMU operational 结果为：accel bias
`[−0.0174527,+0.0276977,−0.0155995] m/s²`，scale
`[0.9996646,1.0068007,1.0070296]`，非正交
`[−0.00223534,+0.00176965,−0.00316793] rad`；gyro bias
`[−0.00640217,+0.000822435,−0.00831762] rad/s`。以 196.156 Hz、中位 0.495805 s
窗估得的逐轴短窗白噪声密度为 accel
`[0.0049735,0.0054053,0.0068535] m/s²/√Hz`、gyro
`[0.0014207,0.0012157,0.0017492] rad/s/√Hz`。该数字口径是短窗 white noise，
不冒充长时 Allan bias instability/random walk；gyro scale/非正交属于已知
角速率转台的通用参考方法，不在该 operational 结果口径内。

MID-360S `CustomMsg` 的每点 `offset_time` 与 200 Hz 内置陀螺仪用于
扫描末参考的 rotation-only deskew，并带入上表官方 IMU 杠杆臂所产生的
确定性旋转分量。真实动态包的同一预定义协议下，高旋转主平面 P95
从 **70.04 mm 降到 20.39 mm**，70/70 个接受扫描全部改善；低旋转对照为
17.255→17.262 mm，基本不变。同点 ablation 中，无杠杆臂到有杠杆臂的
P95 为 20.21→19.48 mm（组中位额外约 3.6%，paired median 约 2.8%）。
`offset_time` 有效率 100%，IMU 完整覆盖 646/647 帧。该实现不声称平台平移
补偿或完整 6DoF deskew。完整 A/B 在
`results/mid360s_deskew_validation.json`。

GUI 的「标定结果」当前共 **11 项**。另一页「标定参考」保留 **7 项**
适用于他人、其他设备或不同部署的方法目录；它们不是本 rig 的
pending/rework，也不属于版本迭代。

---

## 下游验证 — FAST-LIVO2 实时建图（2026-09-19）✅

标定结果第一次被一个不属于本仓库的算法**原样**消费。FAST-LIVO2（`hku-mars/FAST-LIVO2@0d2c034`，ROS 1 noetic 容器）
的配置由脚本从 `results/*.json` 生成，没有手抄数字：

| FAST-LIVO2 参数 | 来源 | 说明 |
|---|---|---|
| `extrin_calib/Rcl, Pcl` | `mid360s_d435i_extrinsic.local.json` → `T_camera_lidar` | 代码里 `Rci = Rcl·Rli`，即 Rcl/Pcl 就是 T_camera_lidar，同向直接用 |
| `extrin_calib/extrinsic_T, R` | `mid360s_lidar_imu.json` → `T_lidar_imu` | (0.011, 0.02329, −0.04412) m，R = I |
| `time_offset/img_time_offset` | `mid360s_d435i_timesync.json` → `time_offset_lidar_to_d435_depth_ms` | 代码 `t = stamp + off`，标定约定 `t_depth = t_lidar + off`，取反得 **+5.989 ms**；标定的是深度流，彩色流可能再差几 ms |
| 相机内参 | `factory_params.json` → `rgb_1280x720` | 必须用 1280×720，640×480 是裁剪不是缩放 |

**结果**（同一 bag，`sha256 22c204e5…`，105 s，三路零丢帧零乱序）：

| 回放条件 | effective 中位 / 最低 | residual 中位 | 终点距起点 | 离起点最远 |
|---|---|---|---|---|
| 1× | 86.4 % / 33.3 % | 0.019 m | 1.45 m | 1.99 m |
| 3× | 86.6 % / 38.4 % | 0.019 m | 1.45 m | 2.11 m |
| 1× + rviz 软件渲染 + 同时录包 | 86.2 % / 40.4 % | 0.019 m | 1.47 m | 2.14 m |

三条轨迹一致到 0.15 m 以内：算法对该输入是确定的，算力余量 ≥ 3×。GIF：`results/fastlivo2_mapping_mid360s_d435i.gif`；
全部字段与判据：`results/fastlivo2_downstream_validation.json`。

**实时踩到、离线无法复现的三种发散**（都不是标定问题）：玻璃/窗户的多回波几何矛盾（用只保留第 0 回波缓解，
玻璃场景 effective 0.7 % → 45 %）；设备放下后所有点挤在 2 m 内的退化（不可恢复，FAST-LIVO2 无重定位）；
IMU 初始化窗口 0.15 s 太短、运动中估歪重力导致位置二次发散（改为 2 s）。
教训：**录包要一直录到放下之后**，失败瞬间才是最有价值的数据。

## 下游验证 2 — Coco-LIC 同一份录制（2026-09-19）✅

第二个消费者换成结构完全不同的算法：Coco-LIC（连续时间 B 样条 LICO，`APRIL-ZJU/Coco-LIC`，经 `KaiFeng-Frank/Coco-LIC-ROS2-Jazzy` 运行），
输入是同一份 bag（`sha256 22c204e5…`）转成的 rosbag2。标定字段的映射：

| Coco-LIC 参数 | 来源 | 说明 |
|---|---|---|
| `camera.yaml: CameraExtrinsics` | `T_camera_lidar · T_lidar_imu` | Coco-LIC 直接要相机→IMU，两个发布矩阵复合，平移 (0.0419, −0.0341, −0.0317) m |
| `lidar.yaml: lidar0/Extrinsics` | `mid360s_lidar_imu.json` → `T_lidar_imu` | 与 FAST-LIVO2 相同 |
| `camera.yaml: img_time_offset` | `mid360s_d435i_timesync.json` | 同一约定、同一个数 **+5.989 ms** |
| 相机内参 / 尺寸 | `factory_params.json` → `rgb_1280x720` | `image_width/height` 必须写 1280×720，见下面的坑 |

**结果**（与 FAST-LIVO2 的 1× 回放轨迹按时间关联 1370/1376 对，只对齐 yaw+平移；没有真值，是互证不是精度）：

| 运行 | 终点距起点 | 路径长 | 对 FAST-LIVO2 RMSE / 中位 / 最大 |
|---|---|---|---|
| FAST-LIVO2（基线） | 1.46 m | 26.97 m | — |
| Coco-LIC LICO，同配置两次 | 1.45 / 1.48 m | 22.7 / 23.0 m | 0.154 / 0.179 m · 0.09 / 0.10 m · 0.76 / 0.90 m |
| Coco-LIC LIO-only | 1.45 m | 22.5 m | 0.135 m · 0.07 m · 0.65 m |

分段看，90 % 的时间两者差 5–10 cm，最大值每次都在 70–80 s 靠窗的一段，是谁偏了说不清。Coco-LIC 多线程、同配置两次差 0.025 m，
3 cm 以内的差别是噪声。路径长短 15 % 是 FAST-LIVO2 离散位姿的抖动（单步最大 123 mm 对 15 mm）。
GIF：`results/cocolic_mapping_mid360s_d435i.gif`（与 FAST-LIVO2 的 GIF 同一视角）；全部字段：`results/cocolic_downstream_validation.json`。

**这次验证暴露的坑（与标定无关，但靠标定的"颜色对不对"发现）**：Coco-LIC 上游把宽 640/1280 的图一律缩成 640×512，
D435i 的 1280×720 被非等比压扁而内参仍按 1280×720 用，去畸变图 64 % 全黑、点云 63 % 纯黑。
跑通、轨迹也像样，只有地图颜色不对——**下游验证要看到彩色地图为止**，轨迹数字对不代表相机那一路真的接上了。
连同其它四个 360° 雷达上的上游缺陷一起修在移植仓库里。

## 阶段 1 — RGB 相机内参 ✅

`data/cam_rgb-camchain.yaml`(Kalibr 格式,可直接被 VINS / ORB-SLAM 等消费)

```yaml
camera_model: pinhole
distortion_model: radtan
intrinsics:        [884.7802, 883.8677, 652.1559, 373.3638]   # fx fy cx cy
distortion_coeffs: [+0.110189, -0.198349, -0.001240, +0.001478]  # k1 k2 p1 p2
resolution:        [1280, 720]
```

参数不确定度(Kalibr 输出):`fx ±3.40  fy ±3.45  cx ±1.28  cy ±1.52`
畸变系数不确定度:`±0.00325  ±0.00666  ±0.00049  ±0.00048`
重投影误差 std:`0.564 / 0.642 px`,26 帧中 19 帧被采用

### 与出厂值对比

| | 出厂 | 本次标定 | 差 |
|---|---|---|---|
| fx | 914.196 | 884.780 | −29.4 px |
| fy | 912.508 | 883.868 | −28.6 px |
| cx | 652.022 | 652.156 | **+0.1 px** |
| cy | 378.636 | 373.364 | −5.3 px |
| 畸变 | 全 0(未提供) | k1=+0.110 k2=−0.198 | — |

`cx` 吻合到 0.13 像素,说明标定流程本身可靠。`fx/fy` 低 3.2% 的推测原因:
出厂使用零畸变模型拟合,而该镜头中段有 +1.2% 的畸变无处安放,被吸收进了焦距。

### 畸变量级(此镜头画面只到归一化半径 r≈0.85)

| 位置 | r | 径向畸变 | 位移 |
|---|---|---|---|
| 中心 | 0.000 | 0.00% | 0.0 px |
| 垂直边缘中点 | 0.392 | +1.23% | 3.7 px |
| 水平边缘中点 | 0.710 | +0.52% | 5.7 px |
| 图像四角 | 0.811 | −1.33% | 10.7 px |

k1 与 k2 反号,零点在 r≈0.75 —— **波浪形畸变**,单靠 k1 一项无法同时拟合中段和边缘。

---

## 阶段 2 — 双目 IR 内外参 ✅

`data/cam_ir-camchain.yaml`(1280×720,IR 双目)

```
cam0  fx=641.140  fy=643.233  cx=631.518  cy=365.416   ±[2.16, 2.21, 1.41, 1.37]
cam1  fx=640.435  fy=642.794  cx=632.239  cy=365.884
出厂  fx=fy=639.189  cx=633.527  cy=365.283
畸变 cam0  k1=-0.0111 ± 0.0020   k2=+0.0052 ± 0.0017
两相机旋转 q=[-0.00104, 0.00085, 0.00005, 0.99999]  ≈ 0.14° 残余
重投影误差 std 0.69 / 0.73 px    24 帧,双目共视 15
```

### ⭐ 基线验证 —— 全流程唯一有独立真值的量

```
标定基线  50.148 mm ± 0.169
出厂基线  50.228 mm          (Intel 产线用专用设备标定)
差        0.080 mm = 0.16% = 0.47σ
```

这一个数字同时确认了四件事:`tagSize=35.2mm` 正确(基线尺度完全由它决定)、
`tagSpacing=0.3` 正确、采集与解算无系统性错误、Kalibr 配置正确。

其他两个发现:
- **IR 畸变不是严格零**:k1=-0.0111 是 5.5σ 显著非零,硬件 rectification 很好但不完美。
  量级比 RGB 的 k1=+0.110 小 10 倍,工程上可忽略但不是零。
- **fx≠fy(641.14 vs 643.23)**,而出厂给严格相等 —— 那个相等是 rectification 的产物,
  不是物理真实。

---

## 阶段 3 — 深度质量核查 ✅

`results/depth_check.json` / `results/depth_check.png`

```
视差噪声        0.076 ~ 0.086 px      (D435 典型 0.05~0.15,健康)
目标自身不平整度 0.70 ~ 0.72 mm        (两轮独立测量吻合到 3%)
系统性畸变       无(残差为均匀随机噪点,无同心圆/条纹)
噪声空间相关尺度  10~20 px             (故小邻域平均无法有效降噪)
深度单位         1.000 mm/LSB
```

误差模型 `plane_rms² = (a·z²)² + flatness²`,外推:

| 距离 | 1.0 m | 1.5 m | 2.0 m | 3.0 m | 4.0 m |
|---|---|---|---|---|---|
| 深度噪声 | 4.1 mm | 9.1 mm | 16.3 mm | 36.6 mm | 65.0 mm |

误差随 **z² 增长**。0.45 m 以内测的主要是目标本身有多平,不是相机精度。

---

## 阶段 4 — cam-IMU 外参 ✅

### 4a. IMU 噪声参数 ✅  `results/allan_imu.yaml` / `results/allan.png`

3.00 小时静置 @ 199.64 Hz,2155726 样本(`data/imu_allan.bag`,770M)

```yaml
accelerometer_noise_density: 1.183355e-03   # m/s²/√Hz
accelerometer_random_walk:   5.943867e-05   # m/s³/√Hz
gyroscope_noise_density:     1.986981e-04   # rad/s/√Hz
gyroscope_random_walk:       3.236584e-07   # rad/s²/√Hz
```

各轴分解(取平均写入 yaml,Kalibr 只接受各向同性):

| | gyro x | gyro y | gyro z | accel x | accel y | accel z |
|---|---|---|---|---|---|---|
| 噪声密度 N | 1.248e-4 | **3.978e-4** | 7.350e-5 | 1.059e-3 | 1.179e-3 | 1.312e-3 |
| 随机游走 K | 3.682e-7 | 4.098e-7 | 1.929e-7 | 3.680e-5 | 6.680e-5 | 7.472e-5 |

⚠ **gyro Y 轴噪声是 X 的 3.2 倍、Z 的 5.4 倍**,是器件各轴差异不是偶然
(60 秒冒烟测试就已显现)。VIO 里三轴用同一参数会过度信任 Y 轴,保守起见可按最大轴取。

数据完整性:逐样本最大跳变 0.106 m/s²(无 >0.5 者),|a| 一分钟滑窗漂移
9.8676~9.8803(跨度 12.7 mm/s²)—— 全程未被碰过。仅第 165 分钟处两次断流
(1017ms / 661ms),215 万样本中可忽略。

Kalibr 官方建议把上述值放大 5~10 倍再用于标定(实际工况的振动温漂远超静置)。

### 4b. cam-IMU 外参 ✅  `data/camimu-camchain-imucam.yaml`

采集:IR 双目 1280×720@30(bag 20Hz)+ IMU 199.5Hz,90 秒,曝光锁 4ms/增益 120
数据质量:gyro 三轴 std 48/82/59 °/s(三轴全充分)、削顶 0.08%、开头静止 0.12 °/s、
tag 中位 28/36、清晰度 82~91。

```
T_cam_imu (cam0)  平移 [+11.14, -16.39, -17.93] mm   |t| = 26.73 mm
                  旋转 ≈ 单位阵,等效角 0.82°
T_ic (cam0→imu)   平移 [-11.48, +16.03, +18.04] mm
时间偏移          cam0: 4.0376 ms    cam1: 4.0166 ms
```

残差:重投影 cam0 median 0.426 / std 0.822 px,cam1 median 0.429 / std 0.753 px
      gyro 0.0115 rad/s,accel 0.107 m/s²

#### 三道独立自洽验证

| 验证 | 结果 |
|---|---|
| 两相机时间偏移之差 | **21 μs**(硬件同步,理论应相同;Kalibr 分别解出) |
| 解出的重力模长 | **9.8070** vs 标准 9.80665 m/s²,**差 0.004%**(标定从未被告知这个数) |
| 立体基线保持 | 50.148 mm,与阶段 2 一致 |

#### ⚠ 平移量的可信度只到"中"

出厂机械外参 IR左→IMU 是 `[-5.52, +5.10, +11.74] mm`(|t|=13.94),
与我们的 `[-11.48, +16.03, +18.04]`(|t|=26.73)**符号方向一致但量级差近一倍**。

出厂值存疑的证据:**accel 和 gyro 给出了完全相同的平移** —— 真实器件里两个敏感元
在芯片不同位置,不可能分毫不差,说明那是机械设计标称值而非逐台实测。

但我们的值也不能全信:**平移外参是 cam-IMU 标定里最弱的一环** ——
旋转外参靠角速度与视觉旋转直接对齐,信号强;平移外参要靠"旋转产生的杠杆臂加速度"
间接解出,需要强的旋转-平移耦合激励。本次 accel std 仅 2.5~3.5 m/s²。

**信心分层:时间偏移(高)/ 旋转(高)/ 平移(低)。**

### 二次独立采集的交叉检验(已做,结论:平移不可信)

第二段 `camimu2.bag`:60s,刻意增强平移激励(accel std 4.8/4.0/5.7,较第一段 +40~60%)。

```
                第一段(90s)   第二段(15~35s)      差
T_ic 平移 x        -11.48        -19.28        -7.80  mm
T_ic 平移 y        +16.03         +2.80       -13.23  mm
T_ic 平移 z        +18.04        +24.94        +6.90  mm
|t|                 26.73         31.65        +4.93  mm
旋转角              1.092         1.083        -0.009 deg
两次旋转之差                     0.529 deg
时间偏移           4.0376        4.5012       +0.4635 ms
```

- **旋转 ✅**:两次差 0.53°,旋转角本身仅差 0.009° —— 可冻结使用
- **时间偏移 ✅**:差 0.46ms —— 可冻结使用
- **平移 ❌**:y 分量差 13.2mm,而该量本身仅 26mm,**相对误差 50%**

**平移解不准是结构性的,不是采集手法问题**:平移外参靠"IMU 偏离光心、随旋转产生的
杠杆臂加速度"间接解出;D435i 的 IMU 距光心仅 2~3cm,杠杆臂过短,信噪比从物理构型上
就不足。第二段为增强激励转得更猛,反而撞上样条带宽上限(见下)并赔上运动模糊
(重投影残差翻倍:median 0.426 -> 0.812 px)。

**使用建议**:平移取第一段值并标注 ±10mm 量级不确定度;**在 VIO 中开启在线外参估计**
(VINS-Mono/Fusion 均支持),把该量交给运行时收敛,优于冻结一个不准的离线值。

#### 噪声参数偏乐观(实测验证了官方经验法则)

```
归一化残差   Gyroscope 4.09    Accelerometer 6.42     (应在 1 附近)
```

实际噪声比 Allan 静置值大 4~6 倍,正落在 Kalibr 官方建议的"放大 5~10 倍"区间。
静置测的是传感器本身,实际工况还有振动、温漂、安装应力。

采集(**用彩色相机**:IR 需关发射器后靠环境光,室内 LED 下可能太暗;
且彩色内参已标定,Kalibr 可当已知量只解外参):

```bash
$P tools/record.py cam_imu --color -o data/camimu.bag -t 90 \
   --width 1280 --height 720 --fps 15 --bag-hz 15
./calibrate_imu_cam.sh data/camimu.bag
```

采集要领:板子固定、相机手持、**开头静止 3 秒**(初始化重力与零偏)、
六自由度逐个激励各 4~5 次、动作顿挫非匀速、板子全程不出画。
最易漏 **roll(绕光轴拧)** —— 缺一个旋转轴该方向外参不可观测。

---


## IMU 内参校正的对照实验(否定结果)

六面静置法标出加速度计内参后(见文末「加速度计内参标定」一节;`a_true = T·K·(a_meas − b)`,12 姿态,|a| 离散从
129 mm/s² 降到 3.76 mm/s²,34 倍),一个自然的猜想是:cam-IMU 标定里那个
偏高的归一化残差(accel 6.42、gyro 4.09,理论上应接近 1)是因为 IMU 内参没标。
2.829° 的 z–x 非正交看起来足够解释它。

于是做了严格对照:**同一份 `camimu.bag`,只改 IMU 观测的前处理**,其余
(靶标、图像、Kalibr 版本、初值、B-spline 配置)全部不动。

| | 校正前 | 校正后 | 比值 |
|---|---|---|---|
| 归一化 gyro 残差 | 4.087 | 4.049 | 0.99× |
| 归一化 accel 残差 | 6.416 | 5.964 | **0.93×** |
| 重投影 cam0 (px) | 0.618 | 0.612 | 0.99× |
| T_ic 平移 y (mm) | 16.03 | 11.04 | −4.99 |
| \|t\| (mm) | 26.73 | 24.57 | −2.16 |
| 旋转角 (°) | 1.092 | 0.843 | −0.249 |
| 时间偏移 (ms) | 4.0376 | 4.0494 | +0.012 |

**猜想被证伪。** accel 残差只降了 7%,gyro 和重投影几乎不动。如果 2.829° 非正交
是残差的主要来源,校正后应该看到量级上的变化,而不是 7%。

原因在于:非正交在**静置**时表现为 |a| 的固定偏差(1.3%),这是六面法能看见、
也确实修好了的部分;但在**运动**中,Kalibr 的 accel 残差是"实测加速度 vs 由
位姿 B-spline 二阶导预测的加速度"之差,里面同时装着运动模糊带来的视觉观测误差、
实际工况下远超 Allan 静置值的振动噪声、以及帧间时间同步抖动。非正交只是其中一项,
而且 Kalibr 的零偏随机游走本来就会吸收掉一部分。

**平移仍然不收敛。** 三次独立解的 |t| 分别是 26.73 / 31.65 / 24.57 mm,散布几乎
和量本身同量级。这坐实了此前的判断:分歧来自 IMU 距光心仅 2~3 cm 的杠杆臂,
不是标定模型缺项 —— 校正 IMU 内参改善不了这个信噪比。

**时间偏移是这次唯一被加固的量:** 4.0376 → 4.0494 ms,差 12 μs。它对 IMU 前处理
不敏感,可以冻结使用。

**可冻结 / 不可冻结:**

- 冻结:`T_cam_imu` 的**旋转**、`timeshift_cam_imu`
- 不冻结:`T_cam_imu` 的**平移** —— 交给 VIO 在线估计,用本文的 24.6 mm 作初值即可
- 加速度计内参本身**值得用**(静置 |a| 改善 34 倍,对姿态初始化和重力对齐有实际收益),
  只是它解决的不是 cam-IMU 残差这个问题



## 深度非线性:三条传统路线受限,残差结构最终收口(2026-08-28)

影响分析里原先唯一"未定界"的一行是深度非线性(阶段 3 只测到 0.92 m)。这次专门
去定它。按整体趋势、外部参考尺和量化核算走完前三条路时,只能得到**在 D435i +
A4 靶标 + 普通房间的条件下定不出优于厂商谱面(2%@4 m)的界**。这是传统测法受
rig 可观测性限制,不是分析失败;随后逐点查看残差结构找到了被二次模型漏掉的信号,
并在本章「终局」中把总量收口到 <1.3%@4 m。

### 路一:斜置平面法

让真实平面斜着铺满画面,单帧内就有距离梯度 —— 距离跨度靠角度换,不靠空间。
真实平面被非线性测成曲面,平面拟合残差沿距离的趋势就是非线性的读数。
纯尺度误差在这里检测不到(整体缩放后平面还是平面),平面拟合还会吸收线性倾斜,
剩下的正好是"非线性"的定义。

失败在分辨力依赖距离跨度。合成验证:

| 连续段 | 远近比 | 零注入时的 95% 上界 |
|---|---|---|
| 1.1~2.5 m | 2.27× | 5.96% |
| 1.1~3.0 m | 2.73× | 4.03% |
| 1.0~3.5 m | 3.50× | 2.92% |
| 1.2~4.2 m | 3.50× | **1.60%** |

要压到谱面(2%)以下需要看到 4 m 外的连续平面。实测房间里墙面 1.2 m 后就拐弯,
地面能给到 2.5 m —— 上界卡在 5~6%,等于没测。

**判据的两次修正(都是自己踩的)**:先只看 min/max 跨度,被 19.45 m 的离群点骗过;
改成等宽分箱查空档后,又漏了绝对距离 —— 0.27~0.94 m 的 3.58× 比例好看,
但杠杆臂太短,对"3~4 m 是否还成立"没有发言权。最终判据是三条并列:
连续段远近比 ≥2.5×、最远 ≥2 m、内点 ≥20000。

### 路二:AprilGrid PnP 参考尺

靶标几何是已知真值,PnP 解出的距离当参考,直接读深度的**绝对**偏差 ——
精度不依赖距离跨度,只依赖靶标在画面里占多少像素。

顺带解决了发射器两难:**散斑是 850 nm 红外,RGB 的 IR-cut 滤镜基本挡掉了它**。
所以发射器全程全功率(深度拿最好信噪比),同时 RGB 上的靶标干干净净,
深度用 `rs.align` 投到 RGB 视角同像素直读。比交替帧更省事,也不用跟 IR 干净帧的
暗光较劲(交替模式下相机自带 AE 会在明暗帧之间震荡,干净帧长期欠曝 —— tags 0/36)。

实测 1 m 处:深度读数 1.0020 m,PnP 0.9878 m,**偏差 +14.2 mm (+1.43%)**。

但这个数字不能用。重投影残留 3.39 px(修正角点顺序前是 45.7 px),
换算成距离不确定性约 1.6% —— 和被测量同量级。根因:A4 靶标在 1 m 处
tag 只有 27 px,亚像素角点检测有系统性内缩。证据是扫描 tagSpacing 时
重投影单调下降到 0.38 都不回头(真值 0.30,四方确认过),说明检测偏差被
spacing 吸收了,不是几何模型错。

**自测的教训**:合成自测只注入 0.1 px 随机噪声,报出 0.02% 的 PnP 精度,
过于乐观 —— 真实检测的是**系统性**偏差,随机噪声模型看不见它。

### 路三:disparity 量化核算(唯一不需要外部参照的)

从深度反推 disparity,核算量化本身贡献多少:

```
z16 步长 1 mm    disparity 相邻间隔 ~1/25 px
1 m: 0.093%   2 m: 0.187%   3 m: 0.280%   4 m: 0.373%
```

比待测量小一个量级,**不是主因**,排除。

### 实地验证(2026-08-28,七轮)

推算说「普通房间到不了 4 m」,实地做了七轮把它验实:

- 地面路线:相机搬到桌面高度(约 92 cm)、俯角压到 18 度,几何上该看到 2.8 m,
  实测卡在 2.2 m —— 工具反复报「镜头前 0.9~1.7 m 被挡住」,是地面杂物,不是姿势。
- 墙面路线:贴墙一端顺着墙拍(距离梯度是几何自带的,不用调俯角),受房间尺寸限制,最远 2.4 m。
- 瞬时到过 3.4 m,稳定值 2.4 m,对应界约 7% —— 比厂商谱面(2%)宽三倍。

**这一轮真正的产出是引导工具本身**:`--live` 实时判断你在看墙还是地面
(按法向分流,两者该给的建议完全相反)、反推相机高度与俯角、算出「看到 4 m 要压到几度」、
检测场景层面的遮挡(只看拟合平面会漏掉「镜头前一米就被挡住」—— 那时平面拟合本身好得很)、
单行原地刷新一次只说一件事。

顺带修了两个自己的坑:`g` 同时当几何提示和循环变量(**坑 #12 变量遮蔽的同款复发**),
以及用 ANSI 上移画多行面板在部分终端不生效导致刷屏 —— 改回 `\r` 单行,所有终端都认。

### 终局:信号一直都在,瞎的是二次模型(2026-08-28)

摆位调到单面墙 0.53~2.90 m 后,分箱残差呈现"0.5→1.6 m 爬升 +4.7 mm、
1.75 m 跳变到 −2.3 mm"的结构 —— 每箱标准误只有 0.01~0.12 mm,这是几十个
标准误的确凿信号。而全局二次拟合对它报"未检出,上界 3.77%"。
**「未检出」≠「测不出」:二次模型对阶跃和周期信号失明。**(用户一句
"这个场景怎么就测不出来了"点醒 —— 先看残差形状,再选模型。)

正确的分析把信号分解成两个成分:

**成分一:pixel-locking 锯齿 —— 已归因为器件行为。**
把每点残差换算到 disparity 域、按 1 px 周期折叠:墙上互不重叠的三个距离段
(0.5–0.9 / 0.9–1.6 / 1.6–3.0 m)共享同一条波形(相关 +0.66 / +0.70)——
墙面接缝不可能与 disparity 的小数部分对齐,归因判别在数据内完成,不需要
挪相机。锯齿峰谷 0.049 px,换算成深度误差按 z² 放大:

```
δz(峰谷) = z²·A/(fx·B),A=0.049 px,fx·B=21.24 px·m
1 m: 2.3 mm (0.23%)   2 m: 9.3 mm (0.46%)   3 m: 20.9 mm (0.70%)   4 m: 37.1 mm (0.93%)
```

它是**确定性函数**:知道 disparity 就知道锯齿相位,可查表校正(GUI 的
补偿通道可以直接挂)。z 域那个"1.75 m 跳变"由此完全解释:近处一个 z 箱
平均掉几十个锯齿周期(残差≈0),1.6~1.8 m 处一个箱不足一个周期,锯齿显形。

**校正闭环(标定的最后一步:能校正才叫标完)。**
模板存 `results/pixellock_model.json`,用法:`disp = fx·B/z`,
`z_corr = fx·B/(disp + g(disp mod 1))` —— 注意是**加号**:δdisp 与 δz 反号。
第一版符号写反,校正让锯齿翻倍(54.5→137.7 milli-px,恰好 2 倍,数值自证),
是"校正方向错误会精确加倍"的现成教具。修正后:

```
同源闭环      54.5 -> 29.0 milli-px
独立时刻重采  波形相关 r = +0.971;53.4 -> 32.8 milli-px
```

残余 ~30 milli-px 有归因、无行动:近域是 z16 毫米量化地板(±0.5 mm 台阶,
不是可校正误差);远域是数据分两团(2.0~2.5 m 空缺)时低频偏移在 fold 域的
混叠伪影 —— 按距离域分解证实(近域校正掉 55%,远域"锯齿"147 milli-px 基本
不动,因为它本来就不是锯齿)。GUI 补偿通道已挂载到实时点云。

**成分二:低频弯曲 —— 定界,未归因。** 法向峰谷 6.3 mm(约 ±0.2~0.4%),
候选是墙面自身平整度或器件低频深度畸变,换一面墙即可裁决;量级已低于
关心阈值,留档不追。

**收口:合计 <1.3% @ 4 m(峰谷),低于厂商谱面 2%,非一阶误差项;
主要成分已归因且可校正。** 图:`results/depth_pixellock.png`。

### 当前结论,以及他人需要更强外部绝对界时的参考条件

- **历史传统路线给出的弱界**:1 m 以内未检出显著非线性;+1.43% 的偏差与参考尺 1.6% 的不确定性同量级,不可判定。
- **给其他设备/复现者的更强外部绝对界参考**(任一即可):A1 尺寸靶标(tag ≈70 mm,2.5 m 处仍有 25 px,参考尺不确定性可降到 0.3%);
  或卷尺 + 相机后退法(对着墙测增量线性度,1 m 位移上精度 0.2%,不需要靶标);
  或 4 m 连续地面/墙面视线(斜置平面法直接给 1.6% 上界)。
  这些条件不反向定义当前 rig 的欠项或版本计划。

### 顺带一个发现

`pyrealsense2` 报告的立体基线是 **50.148 mm** —— 与本项目自标定值一致,
而不是本文档别处记的出厂 50.228 mm。SDK 计算深度用的就是前者。

---

## 多径干扰:墙脚虚深定量(2026-08-28)

用深度非线性那份双平面数据(墙⊥地面,90°,墙脚线 1.6~3.0 m 可见)直接做,
零额外采集。方法:双平面 RANSAC → 解析交线 → 每点算(到交线距离 r,深度向
符号残差 δz);基准面用 r>30 cm 的点重估,防止被污染的近角点把基准拉歪。
合成自测:零注入不虚报(±1.5 mm 内),注入 15 mm 可恢复。

实测(SE < 1 mm,信号 30+ 个标准误):

| | 虚深幅度(<8 cm) | 影响半径 |
|---|---|---|
| 地面侧 | **+25.2 mm(测远)** | ~22 cm |
| 墙侧 | −23.4 mm(测近) | ~12 cm |

地面侧是教科书多径:墙弹来的散斑加长光程,靠墙的地面被测远,**融合平均不掉,
ICP 会被它拉偏** —— 判决:交线 20 cm 内深度按成员资格弃权(已入判决规则)。
墙侧的**测近**不是多径能解释的(多径只加长光程),候选是立体匹配支持窗跨面
或踢脚线真实几何 —— 不对称本身入档,未裁决。

## 材质/反射率:初测被混杂正确拒绝(2026-08-28)

思路是不摆材质阵列:IR 亮度就是回波能量读数,任何有明暗的场景按亮度分箱统计
深度有效率即可。实测曲线却**倒挂**:暗端(远墙)有效率 93%,亮端(IR>180)
只有 44% —— 空间分布显示亮/失效像素集中在画面下部的**镜面瓷砖地板**
(13% 散斑饱和)。亮度在本场景编码的是"哪类物体",不是反射率。

处置:给工具加了单峰性混杂检验,曲线回升 >8% 就**拒绝输出门限**并说明需要
受控摆位 —— 宁可不给,不给一个能用但错误的数字(工具自测含混杂 case)。
副产物是可靠的定性证据:镜面反光区近半深度失效。受控版(同距离黑/白/镜面/
半透明并排)作为其他设备和特殊材质部署的「标定参考」保留，不是本 rig
未完成项。

## 靶标

`aprilgrid_6x6_35.2mm.yaml` — AprilGrid 6×6,`tagSize=0.0352 m`,`tagSpacing=0.3`

`tagSpacing` 经四方确认:尺子实测白缝 10.6 mm(=0.3×35.2)、型号名反推、
Kalibr 自身扫描在 0.30 处取重投影误差极小值(0.25→1.93px / 0.30→0.61px / 0.35→1.38px)。

⚠ 不要用 OpenCV `DICT_APRILTAG_36h11` 检测的角点去反推本文件的参数 —— OpenCV aruco
与 Kalibr 内置 apriltag 库对 tag 边界的定义不一致,混用会得出错误的 tagSpacing。

---

## 已知限制

**USB2 —— 已解决,原因是线缆。** 换了根 USB3 线后立刻协商到 5000 Mbps / bcdUSB 3.20
(供电描述符也从 110mA 变 440mA),而且**照样挂在同一个 Genesys Hub 后面**,
所以 Hub 和物理口从头到尾都没问题。USB-C 线外观完全看不出是 USB2 还是 USB3。
解锁:`Infrared[2]` 出现、depth 最高 300fps、IR 多出 1280×800、彩色多出 1920×1080。

---

## ⚠ 采集踩过的坑(比结果更值得记)

### 1. 检测器的自适应阈值窗口必须匹配 tag 的像素尺度

OpenCV `DetectorParameters` 默认 `adaptiveThreshWinSize` 是 3~23 step 10(只试 3/13/23)。
自适应二值化的窗口要与 tag **单个格子**尺度相当;36h11 是 8×8 格,IR 下 tag 约 48px
时每格才 6px,窗口 13/23 会把相邻黑白格一起平均掉,tag 内部图案糊没。

```
同一张 IR 图:  默认 2/36  ->  winSize 3~15 step 2 后 29/36
连 RGB 图也受益:  11/36 -> 24/36
```

已固化在 `tools/capture.py: make_detector()`。**这是本项目最值钱的一条。**

### 2. D435i 的两个 sensor 都 supports(exposure),循环到最后拿到的是 RGB

```python
for s in dev.query_sensors():
    if s.supports(rs.option.exposure): sens = s   # ← 错,最终指向 RGB Camera
```
调它对 IR 流毫无影响。必须认准 `'Stereo' in name or s.is_depth_sensor()`。

**"参数改了但输出完全不变"是最强的信号** —— 4ms 和 33ms 曝光下亮度一模一样,
说明设置根本没生效,而不是这个参数不管用。

### 3. 过曝会毁掉 tag 检测,且与距离无关

```
 4ms  tag 32/36  过曝 13%      16ms  tag 2/36  过曝 73%
 8ms  tag 15/36  过曝 49%      33ms  tag 0/36  过曝 84%
```
白格烧成一片、光晕吃掉黑格边界。曾误判为"距离和采集互斥",实际两端失败同因。

### 4. 工作距离窗口 = 板子尺寸 × 焦距 × 分辨率,窗口可能为空

```
            848×480 (fx=423)      1280×720 (fx=639)
0.4 m     tag 37px  板占 40%     tag 56px  板占 40%
0.6 m     tag 25px  板占 27%  边  tag 38px  板占 27%  OK
0.8 m     tag 19px  板占 20%  小  tag 28px  板占 20%  边
```
848×480 下「板子完整入画」与「tag 够大」几乎没有重叠区。**换分辨率,别硬调距离。**

### 5. RealSense 的 option 是设备级持久的

上次程序设的曝光/增益会留到下次,不随 pipeline 重启复位。采集前显式设置,别依赖默认值。

### 6. 手持采集必须锁短曝光

自动曝光会为了亮度把曝光拉到帧周期上限(15fps 时可达 66ms),tag 边缘糊成灰带。
**噪点可以靠算法吃掉,模糊不行** —— 宁可降增益也不要加曝光时间。

### 7. Kalibr 的 pose spline 是 100 knots/s 硬编码,运动过猛会直接崩

```
Spline Coefficient Buffer Exceeded. Set larger buffer margins!
assert(_bufferTmin <= _time < _bufferTmax) failed
```

每 10ms 一个控制点,运动频率超过这个带宽就拟合不了,系数被推出缓冲区触发 assert,
随后优化发散(残差飙到 cam1 重投影 210px、gyro error 351 —— 大到荒谬反而好认)。
**该 knot 密度无法通过命令行修改。**

两个诱因,都要避:
- **相机与 IMU 时间范围不对齐**:实测图像首帧比 IMU 首条早 12.2ms,那段图像在样条上
  无定义。→ **`--bag-from-to` 首尾各裁 1 秒应作为默认动作**,代价 2/60 数据。
- **运动过猛**:第二段 30~45s 段角速度中位 213 deg/s(峰值 960),远超第一段整体水平。
  → 增强平移激励要靠**加大幅度而非加快速度**。

### 8. pipeline callback 不保证交付 frameset(偶发丢光图像)

混合流(30Hz 图像 + 200Hz IMU)下,librealsense 有时把 IR1/IR2 打包成 frameset,
有时分成两个独立 video frame。只写 `fs = f.as_frameset(); if not fs: continue`
会在后一种时序下**把图像整批丢光**,录出只有 IMU 的 bag,且不报任何错。

**偶发比稳定崩溃更危险**:第一次采集完全正常,第二次一帧不剩。已在 `record.py` 改为
两种交付都接收,并在收尾时对空 topic 明确告警。

### 9. 帧率两头受夹

```
30fps -> PNG 编码 45ms 撑爆 33ms 帧预算(双目 1280×720)
15fps -> 自动曝光放飞到 66ms
```
正解是**把曝光从帧率里解耦**(锁死 4ms),帧率就只用管 CPU 那一头。

### 10. 两个流的样本配对必须用挂起队列,不能"超范围就丢"

把 accel 插值到 gyro 时刻时,gyro 常常跑在 accel 缓冲的最新时刻之后(两个流独立到达)。
写成 `if acc[0].t <= t_gyro <= acc[-1].t` 会丢掉绝大多数样本:

```
实测 90 秒只存下 14 条 IMU(应有 18000 条)—— 丢失率 99.9%
改为 pending 队列挂起等待后:10952 条 / 55 秒 = 199 Hz ✓
```

`record.py` 的 `GyroClockImu` 本来是对的,`record_thermal.py` 里图省事简化掉了。

### 11. 发射器两难在温漂实验里再次出现,解法是分时

```
深度平面(基线漂移)  →  必须开发射器
靶标角点(主点漂移)  →  必须关发射器(散斑糊在 tag 上直接认不出)
```

同一次采集要两者,只能在每个采样点短暂关掉发射器拍一帧干净 IR,立刻开回来。
占空比约 1/10,对温度曲线影响可忽略。**这是 D435i 的结构性限制,调参绕不开。**

### 12. 变量名遮蔽:`q` 既是帧队列又被用作角点数组

```python
q = queue.Queue()                              # 帧队列
...
q = np.concatenate([x.reshape(4,2) for x in c])  # 角点 —— 把队列覆盖了
...
f = q.get()   # AttributeError: 'numpy.ndarray' object has no attribute 'get'
```

崩在下一轮循环,而不是赋值处,排查时容易误判成队列本身的问题。

### 13. 用"全局均值"会淹没亚像素信号

靶标角点漂移最初用所有检出角点的均值当"中心",结果:

```
t= 8.0s  tag=22  中心=(421.38, 285.38)
t=16.2s  tag=23  中心=(443.58, 281.90)   ← 跳了 22 px
```

**那 22 px 全是伪影** —— 每次检出的 tag 子集不同(22/23/24 个),平均的对象都变了。
主点漂移是亚像素级,被完全埋掉。改为**逐 tag 记录、分析时只取全程可见的**之后,
噪底降到 0.05 px。

---

## 温漂建模实验(2026-08-27)

### 与 Allan 方差的关系:要求恰好相反

```
Allan 方差   要求恒温 —— 温度变了就污染随机噪声估计
温漂建模     要求升温 —— 温度不变就没有自变量,什么也拟合不出来
```

Allan 测的是去趋势后的随机部分,温漂测的正是趋势本身。今天 Kalibr 报的归一化残差
4.09 / 6.42(理想应为 1),温漂是其中一个未建模的成分。

### 一次采集能覆盖哪些:判据是自变量是否相同

| 误差 | 自变量 | 并入静置升温采集 |
|---|---|---|
| IMU 零偏温漂 | 温度 | ✅ |
| 基线漂移(深度尺度) | 温度 | ✅ |
| 主点/焦距漂移 | 温度 | ✅(视野里放靶标) |
| 时间同步漂移 | 温度 + **运动** | ❌ 静置激励不出 |
| 深度非线性漂移 | **距离** | ❌ 单一距离无斜率 |
| 多径干扰 | **场景几何** | ❌ |
| 材质反射率 | **材质** | ❌ |
| 卷帘快门 | **运动** | ❌ |

### 位移场分解:主点 vs 焦距

板与相机固定时,tag 角点的像素位移可分解为

```
Δu = Δcx + (Δf/f)·(u − cx)        平移分量 → 主点漂移
Δv = Δcy + (Δf/f)·(v − cy)        径向缩放分量 → 焦距漂移
```

合成验证(注入 0.05 px 检测噪声,20 个 tag):

```
主点 Δcx  真值 0.300 → 恢复 0.293    误差 0.007 px   ✓
焦距 Δf/f 真值  50   → 恢复  69.6ppm  误差 +39%      ✗ 单点不可分辨
```

**焦距分辨率受杠杆臂限制**:位移 = (Δf/f)·r,而 r 最大仅 280 px,
10 ppm 只产生 0.003 px,低于 0.05 px 噪底。单点分辨率约 40 ppm。
但整段升温(25°C 跨度、1080 采样点)拟合斜率时可平均到 ~0.2 ppm/°C。

---

## 发射器交替帧:「发射器两难」的解法(2026-08-27)

D435i 支持 `rs.option.emitter_on_off`,发射器逐帧开关。实测确认可用:

```
高频能量序列  109  609  109  613  104  613 ...     严格奇偶交替
散斑帧 648~714  /  干净帧 105~112                   相差 5.9 倍
两类间隔 = 115.8 倍组内标准差                        不可能分错
```

**四个方案的完整对比:**

| 选项 | VIO 通道 | 深度 | 快门 | 散斑 | 代价 |
|---|---|---|---|---|---|
| a. IR + 发射器关 | ✓ 干净 | ✗ 弱纹理退化 | 全局 ✓ | 无 | 失去主动立体 |
| b. IR + 发射器开 | ✗ 被散斑毒 | ✓ | 全局 ✓ | 毒 | VIO 不可用 |
| c. RGB + 发射器开 | ✓ 光谱解耦(IR-cut 滤掉 850nm) | ✓ | **卷帘 ✗** | 看不见 | 卷帘畸变 |
| **d. IR + 交替帧** | **✓ 干净** | **✓** | **全局 ✓** | 分时 | **实测几乎无** |

**实测代价比理论小:**

```
散斑帧 14.9 Hz  +  干净帧 14.8 Hz     IR 图像流一分为二
深度   28.2 Hz                        ← 没有减半!
```

D400 的 ASIC 在交替模式下仍逐帧输出深度 —— 散斑帧用主动立体,干净帧用被动立体补上。
**只有 IR 图像流被分,深度完全无损。**

**帧类型判别有两条路,都验过:**

1. **metadata `frame_laser_power_mode`** —— 内核 7.0.0-28 自带足够的 UVC metadata 支持,
   **不需要 `librealsense2-dkms`**(第一天跳过它的判断是对的,但理由错了:
   当时以为拿不到 metadata,实际拿得到)
2. **高频能量(Laplacian 方差)** —— 两类相差 115 倍组内标准差,换场景换曝光也稳

**这个方案只在 USB3 下成立**:USB2 时代 848×480 只有 6fps,分半后各 3fps 不可用;
USB3 给到 90fps,砍半仍有 45fps。

**⚠ 热特性会变**:发射器占空比降到 50%,总功耗下降,升温更慢、平衡温度更低。
不影响已标定的温漂模型(自变量是 ASIC 温度而非时间),但**用交替模式重采温漂数据时
温度跨度可能变窄**,拟合会变差。

已接入:`viewer/server.py --alt-emitter`(界面底部四格,散斑/干净并排)、
`tools/record_thermal.py --alt-emitter`(靶标 tag 数从 22~24 提升到 27~29)。

---

## 加速度计内参标定(2026-08-27)

Kalibr 的 IMU 模型只有 `noise_density` 和 `random_walk`,**它假设标度因子为 1、三轴正交**。
这个假设在本机不成立。

### 六面静置法:12 个姿态,9 个未知数

模型 `a_true = T·K·(a_meas − b)`,K 是标度因子对角阵,T 是非正交上三角阵,b 是零偏。
每个静置姿态给一个约束(`|a_true| = 当地重力`),至少 9 个姿态。

```
标度因子   kx=1.000166   ky=0.998119   kz=0.998908     偏离 1 都在 0.2% 内
非正交角   ayz=-0.781°   azy=+0.275°   azx=-2.829°     ← 主要误差
零偏       bx=+0.0495    by=-0.0167    bz=-0.0434 m/s²
当地重力   9.78767 m/s²  (纬度 22.3°,海拔 30m;不能用 9.80665,差 0.2% 会被 scale 吸收)

残差 129 mm/s² → 3.76 mm/s²    改善 34 倍
留一法交叉验证:12 次重解 scale 波动 0.0003,azx 波动 0.033° —— 解稳定
```

### ⚠ 我犯的错:用单姿态数据推标度因子

先前依据 `thermal.npz`(三小时**固定姿态**)测得 `|a|=9.864` 比重力高 0.7%,
判断"加速度计有 0.7% 标度因子误差"。**这个推断方法本身是错的**:

```
单一姿态下 |a| 的偏差 = bias、scale、非正交 的任意组合,数学上不可分离
12 个姿态平均后 |a| = 9.78916 vs 当地重力 9.78767,偏差仅 0.015%
```

**标度因子几乎就是 1。** 真正的误差是 z-x 轴非正交 2.83°,它让 `|a|` 在不同姿态间
摆动 ±1.3%(std 0.129);固定姿态时表现为一个恒定偏差,看起来就像 scale 误差。

**六面法的意义不是测得更准,是把三种误差分离开** —— 单姿态数据无论测多久、多精确,
都分不开它们。

### 采集方式:不需要摆稳

D435i 有些面是弧形放不稳,手持也达不到 0.6°/s 的静止要求。解法是把阈值放宽到
**4°/s**(手持通常 1~3°/s):手抖是高频的,窗口内平均即可压掉;工具自动记录朝向、
按 15° 夹角去重、提示换姿态,**用户不需要记方向**。

实测 12 个姿态三轴正负全覆盖,最近邻夹角中位 44°。

---

## 复现

```bash
P=python   # 你的 d435i-calib 环境里的 python
cd /path/to/d435i_calib

$P tools/check_device.py                                   # 设备/出厂参数/阶段可行性
$P tools/capture.py --stream color -o data/cam_rgb.bag     # 阶段1 采集(--resume 续采)
./calibrate_cam.sh data/cam_rgb.bag                        # 阶段1 标定
$P tools/check_depth.py                                    # 阶段3 深度核查
$P tools/record.py imu -o data/imu_allan.bag -t 10800      # 阶段4 Allan 采集
$P tools/allan.py data/imu_allan.bag                       # 阶段4 噪声参数
```
